def my_selection_sort(t): #t tableau d'entiers
       res=[0]*len(t): #len(t) >=1
       for i in range (len(t)):
				j: index_of_the_smallest(t)
                res[i]: t[j]
                remove(t,j)
        return res